# Course hero images

Drop the photos you sent into this folder with these exact filenames. The
Learning Hub picks them up automatically; if a file is missing the card falls
back to the SVG mark.

| Filename                  | Used by               | Recommended aspect | Notes                                       |
|---------------------------|-----------------------|--------------------|---------------------------------------------|
| `aws.png`                 | AWS Solutions Architect | 16:9 (e.g. 1280×720) | The dark-navy AWS hero you shared           |
| `github-actions.png`      | GitHub Actions        | 16:9               | The dark-navy GitHub + workflow hero        |
| `git-github.png`          | Git & GitHub          | 16:9               | The Git diamond + Octocat composition       |
| _(no FDE image)_          | Forward-Deployed Engineer | — | Uses the SVG monogram — drop `fde.png` here if you want a photo too |

PNG or JPG both work. The card crops with `object-fit: cover`, so anything
close to 16:9 looks right — the focal subject should be roughly centred.

After dropping the files, refresh the Learning Hub. No code changes needed.
